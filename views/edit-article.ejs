<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Article</title>
  <link rel="stylesheet" href="/base.css">
  <link rel="stylesheet" href="/index.css">
</head>
    <body>
        <section class="app">
			<nav class="nav">
				<ul>
                    <li class="settings"><a href="/users/settings">settings</a></li>
					<li class="user"><%= user.username %></li>
                    <li>
                        <button class="logout"><a href="/users/logout">Sign out</a></button>
                    </li>
				</ul>
			</nav>
            <section class="main">
                <button class="back-button"><a href="/author/home">Back to Home</a></button>
                <div class="article-container">
                    <h1>Edit Article</h1>
                    <p>Created: <%= new Date(article.created_at).toLocaleDateString() %></p>
                    <p>Last Modified: <%= new Date(article.last_modified).toLocaleString() %></p>
                    <p>Published at: <%= new Date(article.published_at).toLocaleString() %></p>

                    <form action="/author/article/update" method="POST">
                        <label for="title">Article Title</label>
                        <input type="text" id="title" name="title" value="<%= article.title %>" required>

                        <label for="title">Article Subtitle</label>
                        <input type="text" id="subtitle" name="subtitle" value="<%= article.subtitle %>" required>

                        <label for="content">Article Text</label>
                        <textarea id="content" name="content" rows="10" required><%= article.content %></textarea>

                        <input type="hidden" name="id" value="<%= article.id %>">
                        <button type="submit">Submit Changes</button>
                    </form>
                </div>
            </section>
        </section>
    </body>
</html>

